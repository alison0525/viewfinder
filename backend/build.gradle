plugins {
    id 'java'
    // Java 코드의 기본 품질 규칙을 검사하는 Gradle 내장 플러그인 적용
    id 'checkstyle'
    // 테스트가 실행한 코드 범위를 측정하고 리포트를 생성하는 Gradle 내장 플러그인 적용
    id 'jacoco'
    id 'org.springframework.boot' version '4.1.0'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.viewfinder'
version = '0.0.1-SNAPSHOT'
description = 'backend'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    implementation 'org.springframework.boot:spring-boot-starter-webmvc'
    // Spring Boot 시작 시 Flyway 마이그레이션 자동 실행을 포함한 스타터
    implementation 'org.springframework.boot:spring-boot-starter-flyway'
    // Flyway의 PostgreSQL 데이터베이스 지원 모듈
    implementation 'org.flywaydb:flyway-database-postgresql'
    // JWT 생성·검증에 사용하는 공개 API 모듈
    implementation 'io.jsonwebtoken:jjwt-api:0.13.0'
    // JJWT 내부 구현과 Jackson JSON 처리 모듈을 런타임에만 추가
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.13.0'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.13.0'
    compileOnly 'org.projectlombok:lombok'
    developmentOnly 'org.springframework.boot:spring-boot-devtools'
    runtimeOnly 'org.postgresql:postgresql'
    annotationProcessor 'org.projectlombok:lombok'
    testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-data-redis-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-security-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-validation-test'
    testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
    testImplementation 'org.springframework.boot:spring-boot-testcontainers'
    testImplementation 'org.testcontainers:testcontainers-junit-jupiter'
    testImplementation 'org.testcontainers:testcontainers-postgresql'
    testCompileOnly 'org.projectlombok:lombok'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    testAnnotationProcessor 'org.projectlombok:lombok'
}

tasks.named('test') {
    // JUnit 5 기반 테스트 엔진 사용 지정
    useJUnitPlatform()
    // 테스트 실행 중 활성화할 Spring 프로필 이름 전달
    systemProperty 'spring.profiles.active', 'test'
    // 테스트 성공·실패와 관계없이 현재 커버리지 리포트 생성
    finalizedBy tasks.named('jacocoTestReport')
}

jacoco {
    // 현재 프로젝트 Java 25를 공식 지원하는 JaCoCo 버전 지정
    toolVersion = '0.8.15'
}

tasks.named('jacocoTestReport') {
    // 리포트만 단독 실행해도 테스트 실행 결과가 먼저 생성되도록 연결
    dependsOn tasks.named('test')

    reports {
        // CI 도구와 이후 커버리지 분석 도구가 읽을 XML 리포트 생성
        xml.required = true
        // 개발자가 브라우저로 확인할 HTML 리포트 생성
        html.required = true
        // 현재 사용처가 없는 CSV 리포트 생성 비활성화
        csv.required = false
    }
}
